import { ExamInterface } from "@/components/quiz/ExamInterface";

export default function ActiveExamPage() {
    // In a real app, we would fetch the exam data based on the ID
    return <ExamInterface />;
}
