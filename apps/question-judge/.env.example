# Question Judge Service Configuration

# Shared secret for authentication (required in production)
QUESTION_JUDGE_SHARED_SECRET=your-secret-key-here

# Port to run the service on
PORT=8000

# Environment (development | production)
ENV=production

# Optional: Override the cross-encoder model
# MODEL_NAME=cross-encoder/ms-marco-MiniLM-L-6-v2
