// Legacy Vercel Blob export tests.
// Disabled because the export pipeline no longer uses the Blob code path.

import { describe, it } from 'vitest';

describe.skip('legacy export-check', () => {
  it('is intentionally disabled', () => {});
});
