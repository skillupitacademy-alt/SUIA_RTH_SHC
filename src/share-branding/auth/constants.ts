/**
 * 🔐 AUTH CONSTANTS - SINGLE SOURCE OF TRUTH
 * 
 * Centralized constants for authentication system.
 * Prevents duplication across route files.
 */

/**
 * Fallback API base URLs for each brand
 * Used when gateway routing is not available
 */
export const FALLBACK_API_BASE_RTH = 'https://api.realtutorialhub.com/api';
export const FALLBACK_API_BASE_SKILLUP = 'https://api.skillupitacademy.com/api';