/**
 * Gateway brand config — re-exports from the unified master config.
 * All brand data now lives in: src/share-branding/brandConfig.ts
 */
export type { BrandConfig } from '../../../brandConfig';
export { brands, rthConfig, skillUpConfig } from '../../../brandConfig';
