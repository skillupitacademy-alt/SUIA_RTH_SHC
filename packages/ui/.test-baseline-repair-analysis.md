# TEST BASELINE REPAIR - FAILURE CLASSIFICATION

## Executive Summary

**Total Failures:** 30
- CodeC1Block: 25 failures
- TutorialRenderer: 5 failures

**Phase 2/3 Baseline:** 39/39 PASS (protected)

## Root Cause Analysis

### Category Distribution

| Category | Count | Description |
|----------|-------|-------------|
| C - Incorrect Assertion | 25 | Test expects wrong implementation |
| E - Type/Contract Mismatch | 5 | Test fixture missing required data |

---

## CodeC1Block Failures (25 total)

### Actual Implementation Contract

**Current CodeC1Block.tsx (204 lines):**

```
ROOT: <article data-block-id data-block-type="code" data-block-version="C1">
TITLE: <h1> {page.title} </h1>  ← NOT <span>, NOT <h3>
LANGUAGE: In TerminalWindow title prop: {page.language || 'Code'}
FILENAME: In TerminalWindow title prop: {page.filename} (if present)
CODE: <code> {page.code} </code>  ← NO data-language attribute, NO .language-* class
COPY BUTTON: "Copy" / "Copied"  ← NOT aria-label="copy code to clipboard"
EXPLANATION: Grid with numbered items  ← NOT border-l-2 class
OUTPUT: Optional, rendered when page.output?.value exists
```

**Key Contract Points:**
1. Title is H1, not SPAN (line 107)
2. Language is in TerminalWindow title, NOT as `data-language` attribute on `<code>`
3. Filename is in TerminalWindow title (line 114: `title={page.language || 'Code'}` - NO filename support!)
4. Copy button text is "Copy"/"Copied", NOT aria-label
5. No `id` attribute on article (has `data-block-id` instead)
6. Explanation uses grid layout, not border-l-2 classes

---

### CATEGORY C: INCORRECT ASSERTIONS (25 failures)

#### TEST 2 - Title Element Type
**Failure:** `expect(title.tagName).toBe('SPAN')`
**Actual:** H1 element (line 107)
**Fix:** Change assertion to expect 'H1'
**Provenance:** STALE TEST - implementation correctly uses H1 for semantic heading

#### TEST 5 - Language Attribute
**Failure:** `expect(codeElement).toBeInTheDocument()` where `codeElement = querySelector('code[data-language="python"]')`
**Actual:** Language is in TerminalWindow title, NOT as attribute
**Fix:** Change selector to verify language in title text
**Provenance:** STALE TEST - implementation never had data-language attribute

#### TEST 8, 15 - Filename Rendering
**Failure:** `expect(screen.getByText('hello.py')).toBeInTheDocument()`
**Actual:** TerminalWindow title uses `page.language || 'Code'` - NO filename support!
**Fix:** IMPLEMENTATION BUG - CodeC1Block does not render filename
**Provenance:** TEST CORRECT - implementation regression

#### TEST 10 - Output Description
**Failure:** `expect(screen.getByText(/The text is printed to the console/)).toBeInTheDocument()`
**Actual:** Output section only renders `page.output.value`, NOT `description` field
**Fix:** IMPLEMENTATION BUG - output.description not rendered
**Provenance:** TEST CORRECT - implementation incomplete

#### TEST 16, 17 - Explanation Item Count
**Failure:** `const explanationItems = container.querySelectorAll('[class*="border-l-2"]'); expect(explanationItems).toHaveLength(2)`
**Actual:** Explanation uses grid layout, not border-l-2 class
**Fix:** Change selector to grid structure
**Provenance:** STALE TEST - implementation uses different CSS structure

#### TEST 27-41 - Language Tests (15 failures)
**Failure:** All expect `container.querySelector(\`code[data-language="${lang}"]\`)`
**Actual:** No data-language attribute or .language-* class
**Fix:** Change assertion to verify language in TerminalWindow title
**Provenance:** STALE TEST - implementation never had language attributes

#### TEST 42 - Article ID Attribute
**Failure:** `expect(article).toHaveAttribute('id', 'test-c1-block-id')`
**Actual:** Has `data-block-id` attribute, NOT `id`
**Fix:** Change assertion to expect data-block-id
**Provenance:** STALE TEST - Phase 2 uses data-block-id

#### TEST 44 - Copy Button Accessible Label
**Failure:** `screen.getByRole('button', { name: /copy code to clipboard/i })`
**Actual:** Button text is "Copy"/"Copied", no aria-label
**Fix:** Change to `screen.getByRole('button', { name: /copy/i })`
**Provenance:** STALE TEST - implementation uses visible text

#### TEST 45 - Heading Hierarchy
**Failure:** `expect(container.querySelector('h3')).toBeInTheDocument()`
**Actual:** Uses H1 for title, H2 for sections
**Fix:** Change to expect H1
**Provenance:** STALE TEST - implementation uses correct semantic heading structure

---

## TutorialRenderer Failures (5 total)

### CATEGORY E: TYPE/CONTRACT MISMATCH (5 failures)

#### TEST "4. code" - Unversioned Code Block
**Failure:** Error thrown at TutorialBlockRenderer.tsx:78
**Error:** `Unsupported code block version. Code C1 is required. Received: no version`
**Fixture:**
```typescript
const codeBlock: CodeBlock = {
  id: 'b-code1',
  type: 'code',
  content: { language: 'java', code: '...', filename: 'Main.java' }
}
```
**Fix:** Add `version: 'C1'` to fixture + migrate content to C1 structure
**Provenance:** STALE FIXTURE - architecture requires C1 version

#### TEST "8. definition" - Missing Theme
**Failure:** Error in DefinitionBlock.tsx:60
**Error:** `[DefinitionD1View] Missing required brand theme`
**Fix:** Pass theme prop to TutorialBlockRenderer
**Provenance:** INCOMPLETE FIXTURE - DefinitionBlock requires theme

#### TEST "full document" - Unversioned Code in Document
**Fixture:** Same as TEST "4. code"
**Fix:** Same - migrate to C1
**Provenance:** STALE FIXTURE

#### TEST R2 - Legacy Code Routing Expectation
**Failure:** Tests expects legacy code WITHOUT version to render
**Actual:** TutorialBlockRenderer requires C1 version
**Fix:** Either:
  A. Change test to expect error (matches current architecture)
  B. Restore legacy fallback (NOT RECOMMENDED - weakens architecture)
**Provenance:** TEST/ARCHITECTURE MISMATCH - needs architectural decision

#### TEST R3 - Wrong Heading Selector
**Failure:** `expect(container.querySelector('h3')).toBeInTheDocument()`
**Actual:** C1 uses H1 for title
**Fix:** Change to H1
**Provenance:** STALE TEST

---

## Architectural Decisions Needed

### 1. Legacy Code Support (TEST R2)

**Current Architecture:** TutorialBlockRenderer.tsx enforces C1 version (line 78)

**Options:**
- **A. Maintain strict C1 requirement** (RECOMMENDED)
  - Update TEST R2 to expect error for unversioned code
  - Rationale: Clean architecture, explicit contracts
  
- **B. Restore legacy fallback**
  - Add fallback to CodeBlock for unversioned code
  - Rationale: Backward compatibility
  - Risk: Weakens type safety

**Recommendation:** Option A - Update test to match architecture

### 2. Filename Support in CodeC1Block

**Current Implementation:** Line 114 uses `page.language || 'Code'` - ignores filename

**Options:**
- **A. Add filename support** (RECOMMENDED)
  - Change to: `title={page.filename || page.language || 'Code'}`
  - Rationale: Tests expect it, useful feature
  
- **B. Remove filename from tests**
  - Remove TEST 8, 15 expectations
  - Rationale: Not in current implementation
  - Risk: Loses useful feature

**Recommendation:** Option A - Fix implementation

### 3. Output Description Rendering

**Current Implementation:** Only renders `page.output.value` (line 161-168)

**Options:**
- **A. Add description rendering** (RECOMMENDED)
  - Render `page.output.description` if present
  - Rationale: Part of type contract, useful context
  
- **B. Remove from tests**
  - Update TEST 10
  - Rationale: Simpler implementation
  - Risk: Loses useful feature

**Recommendation:** Option A - Fix implementation

---

## Fix Strategy

### Phase 1: Minimal Production Fixes (APPROVED)
1. Add filename support to CodeC1Block (1 line change)
2. Add output.description rendering (optional field, ~5 lines)

### Phase 2: Test Fixture Corrections (APPROVED)
3. Migrate TutorialRenderer code fixtures to C1 structure
4. Add theme prop to TutorialRenderer tests

### Phase 3: Test Assertion Corrections (APPROVED)
5. Fix TEST 2: H1 not SPAN
6. Fix TEST 5: Language in title, not attribute
7. Fix TEST 16,17: Grid selector not border-l-2
8. Fix TEST 27-41: Language in title assertions
9. Fix TEST 42: data-block-id not id
10. Fix TEST 44: Button text not aria-label
11. Fix TEST 45: H1 not H3
12. Fix TEST R2: Expect error for unversioned code
13. Fix TEST R3: H1 not H3

### Phase 4: File Size Compliance
14. Split ActiveBlockRuntime.test.tsx (640 → <600 lines)

---

## Success Criteria

✅ CodeC1Block.test.tsx: 46/46 passing
✅ TutorialRenderer.test.tsx: 29/29 passing
✅ BlockDOMIdentity.test.tsx: 18/18 passing (preserved)
✅ ActiveBlockRuntime.test.tsx: 21/21 passing (preserved)
✅ All production files ≤600 lines
✅ All test files ≤600 lines
✅ packages/ui type-check: PASS
✅ No backend/API/database/auth changes

---

## Out of Scope

❌ CookieBrand error in apps/api-server (unrelated)
❌ Phase 4 ILS implementation
❌ Tutorial Engine redesign
❌ Backend/API/database/auth/RBAC changes
