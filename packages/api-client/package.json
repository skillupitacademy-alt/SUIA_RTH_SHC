{
    "name": "@quiz/api-client",
    "version": "0.1.0",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "type-check": "tsc --noEmit -p tsconfig.json",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "typescript": "^5"
    },
    "devDependencies": {
        "vitest": "^5.0.0",
        "@vitest/coverage-v8": "^5.0.0"
    }
}
